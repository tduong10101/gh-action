name: build
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: LintTestBuild
        run: |
          npm install
          npm lint
          npm test
          npm build

  
